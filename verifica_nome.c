#include <stdio.h>

int main(int argc, char const *argv[])
{
    char name[10] = "Pastel";
    for (int x = 0; x < name[x]; x++)
    {
        printf("%c ", name[x]);
    }
    // printf("Scanea teu nome");

    return 0;
}
